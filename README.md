# GO PI

GO PI is a Pi Testnet demo app that lets Pioneers test payments, callbacks, and app-to-user flows.

## 🚀 Run locally
```bash
npm install
npm start
```

Expose with:
```bash
npx ngrok http 3000
```
